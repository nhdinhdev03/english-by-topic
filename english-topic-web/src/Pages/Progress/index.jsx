import './Progress.scss';

const Progress = () => {
  return (
    <div className="progress-page">
      <div className="progress-container">
        <h1>Tiến độ</h1>
        <p>Theo dõi điểm số, streak và biểu đồ tiến bộ của bạn</p>
        {/* ProgressBar và progress charts sẽ được thêm ở đây */}
      </div>
    </div>
  );
};

export default Progress;