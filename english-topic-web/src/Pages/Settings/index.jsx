import './Settings.scss';

const Settings = () => {
  return (
    <div className="settings-page">
      <div className="settings-container">
        <h1>Cài đặt</h1>
        <p>Tùy chỉnh giọng nói, tốc độ, accent và các tùy chọn hiển thị</p>
        {/* VoiceSelector và settings options sẽ được thêm ở đây */}
      </div>
    </div>
  );
};

export default Settings;